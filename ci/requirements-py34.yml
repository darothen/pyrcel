name: parcel_model
channels:
    - darothen
dependencies:
    - python=3.4
    - cython
    - future
    - pandas
    - pyyaml
    - scipy
    - numpy
    - xarray
    - pip
    - pip:
      - assimulo