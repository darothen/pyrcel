.. _install:

Installation
============

To grab and build the latest version of the model, you should use ``pip`` and point it
to the source code repository on github:

..
    ``pip install git+git://github.com/darothen/parcel_model.git``

