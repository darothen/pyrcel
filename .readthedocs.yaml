version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
  commands:
    - pip install uv
    - uv pip install --system -e ".[docs]"
    - DOCS_EXECUTE=1 MPLBACKEND=Agg mkdocs build --site-dir $READTHEDOCS_OUTPUT/html --no-strict
